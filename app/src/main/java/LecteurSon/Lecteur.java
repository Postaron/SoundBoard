package LecteurSon;

import android.media.SoundPool;

/**
 * Will be used to simplify the usage of SoundPool class
 *
 * @author postaron
 * @version 1.0
 */

public class Lecteur {
    private SoundPool soundPool = null; //First step
    /*
    Autres solutions :
    - mettre tous les morceaux en raw, pourquoi pas une liste de raw ressource contenant tous les
    morceaux
    - ajout possible de fichiers au fur et à mesure (au bout d'un moment, on remet en dur dans un
    autre APK)
    - ajout des morceaux dans un dossier asset possible : ajout par la suite possible aussi.
     */
}
